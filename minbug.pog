
struct Entity {
    char* name;
    int32 health;
    float32 x, y;
}

void* malloc(uint64 size);

void main() {


    Entity* e = alloc Entity*;
    void(uint32, Entity*[..](float32**), char*) proc = e;

    Entity[] es = malloc((sizeof Entity) * 16);


    for 0 .. 16 {
        es[it].name = it % 2 ? "odd" : "even";
    }

    for 0 .. 16 {
        print(it, ", ", es[it].name, "\n");
    }
}

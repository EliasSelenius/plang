
declare void printf(char* format);

OpaqueType* ot;

type OpaqueType;
type func = void(OpaqueType*);


void take_opaque(OpaqueType* ot) {
    printf("Hello, Opaque\n");
}

void reg(func f) {
    printf("reg\n");
}

int32 main() {

    reg(take_opaque);

    void** p = null;

    take_opaque(p);

    printf("Hello, Sailor!\n");

    return 0;
}
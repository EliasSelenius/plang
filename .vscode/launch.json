{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "c project",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/plang.exe",
            "args": ["deref.txt"],
            "cwd": "${workspaceFolder}",
            // "preLaunchTask": "c_project_build"
        }
    ]
}
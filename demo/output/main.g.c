#include "main.g.h"

int main() {
    float* g = malloc(sizeof(float) * 255);
    char* s = "Hello World";
    char* f = "Hello World";
    Gameobject* obj = malloc(sizeof(Gameobject));
    float myTestNum = (1 + ((1 / 4) * 100));
    while (0) {
        printf("%s", "Looooooooop");
    } 
    printf("%s", "Hello plang");
    foo(12, 5, "test");
    return 0;
} 
void foo(int a, int b, char* str) {
    printf("%s", "foo");
} 
char* bar() {
    if (1) {
        if (0) {
        } 
        if (1) {
        } 
    } 
    if (0) {
    } 
    return 0;
} 
char* getMessage() {
    if (1) {
        return "Hello World";
    } 
    return "Nice";
} 
